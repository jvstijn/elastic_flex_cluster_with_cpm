# Dagboek — 2026-07-17

Dit dagboek beschrijft alle stappen die ik (Claude) op deze dag heb uitgevoerd. Elke dag is een aparte file in `docs/dagboek/`.

## Doel van vandaag
- Een dagboek opzetten waarin alle stappen worden bijgehouden.
- Alle componenten starten via de docker compose file in `docker-local/`.

## Stappen

### 1. Omgeving verkend
- `docker-local/` bevat o.a. `docker-compose.yml`, `.env.example`, `README.md`, `TESTPLAN.md`, `config/`, `scripts/`, `setup/`.
- De stack bestaat uit de volgende services (uit `docker-compose.yml`):
  - `setup`, `certs` — TLS-certificaten genereren
  - `es-central` (:9200), `es-remote-a` (:9201), `es-remote-b` (:9202) — 3 Elasticsearch clusters
  - `init` — trial license, CCS, bootstrap pipeline
  - `kibana` (:5601)
  - `kafka`, `kafka2`, `kafka3` — Kafka cluster (3 brokers)
  - `kafka-ui` (:8080)
  - `logstash-beats` (:5044), `logstash-central`, `logstash-remote-a`, `logstash-remote-b`, `logstash-router` — Logstash instances
  - `metricbeat-central`, `metricbeat-remote-a`, `metricbeat-remote-b` — monitoring
  - `dod-elastic`
- Docker draait (`docker info` OK).

### 2. Dagboek opgezet
- Directory `docs/dagboek/` aangemaakt.
- Deze file (`2026-07-17.md`) aangemaakt.

### 3. Configuratie voorbereid
- Er bestond nog geen `.env` in `docker-local/`. `.env` aangemaakt op basis van `.env.example` (standaard wachtwoorden, `STACK_VERSION=8.19.16`, `LICENSE=trial`).

### 4. Componenten starten (eerste poging)
- `docker compose up -d` uitgevoerd in `docker-local/`.
- Kafka-cluster (`kafka`, `kafka2`, `kafka3`) + `kafka-ui` starten en worden **healthy**.
- De 3 Elasticsearch-nodes (`es-central`, `es-remote-a`, `es-remote-b`) **crashen** direct bij opstarten.
- Oorzaak uit de logs: `indexCreatedVersionMajor is in the future: 10`. De data-volumes bevatten state geschreven door een ES **9.x** (Lucene major 10), maar de stack draait op `8.19.16`. Elasticsearch ondersteunt geen downgrade → nodes stoppen met exit code 1.
- Vervolgcomponenten (`init`, `kibana`, alle `logstash-*`, `metricbeat-*`) blijven daardoor in `created` staan (dependency failed).

### 5. Branch gewisseld
- Op verzoek gewisseld van `main` naar branch `mod-imre3006` (`git checkout mod-imre3006`).
- Ook op deze branch staat `STACK_VERSION=8.19.16`, dus het volume-probleem blijft identiek.
- Openstaande keuze aan gebruiker: bestaande ES data-volumes wissen (verse start op 8.19.16) óf de stack upgraden naar 9.x om de data te behouden.

### 6. Keuze: upgraden naar 9.x → 9.4.2
- Gebruiker koos voor **stack upgraden** (data behouden). Vervolgens versie opgegeven: **9.4.2**.
- `STACK_VERSION=9.4.2` gezet in `docker-local/.env`.
- `docker compose up -d` opnieuw uitgevoerd: de 3 ES-nodes worden nu **healthy** op 9.4.2, de bestaande 9.x-data wordt correct gelezen.

### 7. Orphans opgeruimd
- De branch-compose (`mod-imre3006`) definieert minder services dan `main` (o.a. één `kafka` i.p.v. drie, en geen `logstash-central/remote-*/router`, geen `kafka-ui`).
- Achtergebleven containers van de `main`-compose (`logstash-central`, `logstash-remote-a/b`, `logstash-router`, `kafka2`, `kafka3`, `kafka-ui`) waren orphans.
- `docker compose up -d --remove-orphans` uitgevoerd → orphans verwijderd.

### 8. Logstash-config gefixt voor 9.x
- `logstash-beats` en `logstash-managed` crashten: `Setting "http.host" doesn't exist`.
- In Logstash 9.x is `http.host` hernoemd naar `api.http.host`. In beide files aangepast:
  - `config/logstash-beats/logstash.yml`
  - `config/logstash-managed/logstash.yml`

### 9. Built-in user wachtwoorden hersteld
- Na de config-fix gaf `logstash-managed` een **401** voor `logstash_system`. Oorzaak: de `init-state` volume (hergebruikt) bevat een "already initialized"-marker, waardoor `init-stack.sh` het (her)zetten van wachtwoorden oversloeg; de wachtwoorden in de oude ES-data matchen niet met de nieuwe `.env`.
- `elastic:changeme-elastic` werkte wel (200). Via de `elastic` superuser de built-in wachtwoorden hersteld naar de `.env`-waarden:
  - `logstash_system` → `changeme-logstash` (401 → 200)
  - `kibana_system` → `changeme-kibana` (401 → 200)
- `metricbeat` (3x) authenticeert en indexeert correct (kleine mapping-drop van ~2 events/10s, niet blokkerend).

### 10. Eindstatus — alle componenten gestart
Cluster: **status yellow** (normaal bij single-node; replicas unassigned), 1 node, ES **9.4.2**.

| Service | Status |
|---------|--------|
| es-central (:9200) | running (healthy) |
| es-remote-a (:9201) | running (healthy) |
| es-remote-b (:9202) | running (healthy) |
| kibana (:5601) | running (healthy) |
| kafka (:9092) | running |
| logstash-beats (:5044) | running |
| logstash-managed | running (zie openstaand punt) |
| metricbeat-central / -remote-a / -remote-b | running |
| setup, init | exited (0) — one-shot, verwacht |

### Openstaand punt (niet blokkerend voor opstarten)
- `logstash-managed` draait, maar krijgt herhaald **403** op `/_logstash/pipeline?id=*`. `logstash_system` authenticeert (200 op `/`), maar de built-in `logstash_system`-rol in ES 9.x bevat geen `manage_logstash_pipelines`-privilege. `init-stack.sh` kent dit privilege ook niet toe.
- Gevolg: centralized pipeline management laadt (nog) geen pipelines. Dit is een CPM-config-laag die normaal via de ansible-bootstrap wordt ingericht (zie `docker-local/README.md`).
- Mogelijke fix (nog niet uitgevoerd, ter afstemming): een custom rol met `manage_logstash_pipelines` toekennen aan de management-gebruiker, of de CPM ansible-bootstrap draaien.

### 11. Ansible-role `elastic_cpm` uitgerold
- Verzoek: de role `elastic_cpm` uitrollen. Dit is de volledige CPM-installatie (`ansible/site.yml` → role `elastic_cpm`, inventory `inventories/local`, host `localhost` local connection).
- Ansible was niet geïnstalleerd → geïsoleerde venv aangemaakt in de scratchpad en `ansible-core 2.21.2` geïnstalleerd (systeem niet vervuild). Geen custom collections nodig (alleen `ansible.builtin.uri`).
- Preventieve check: `group_vars/all.yml` gebruikt `cpm_monitoring_index: ".monitoring-es-8-*"`; metricbeat 9.4.2 schrijft nog steeds naar `.monitoring-es-8-mb-*`, dus pattern klopt. Ansible-lookups (elastic-wachtwoord uit `.env`, poort) werken.
- Uitgevoerd: `ansible-playbook -i inventories/local/hosts site.yml` → **`ok=61, changed=0, failed=0`**. `changed=0` = alle CPM-resources bestonden al (uit de eerdere 9.x-install op deze data-volume); role is idempotent en bevestigt de gewenste staat.
- Verificatie:
  - 12 `cpm-*` config-indices aanwezig (o.a. `cpm-cluster-registry`=3, `cpm-pipeline-state`=309, `cpm-scores`=116, `cpm-pipeline-templates`=2).
  - 5 ML-jobs (`cpm-*`), 8 watchers, 1 actieve `cpm-watcher-webhook` API-key.
  - 4 Kibana-dashboards: platform-overview, pipeline-assignments, pipeline-history, stream-locks.

### Openstaand punt (ongewijzigd): logstash-managed 403
- Het uitrollen van `elastic_cpm` lost de logstash-managed **403** niet op. De role maakt een API-key mét `manage_logstash_pipelines` voor de **watcher-webhook**, maar `logstash-managed` gebruikt voor centralized pipeline management de user **`logstash_system`** (`xpack.management`), en die built-in rol mist dat privilege. 403 blijft (24 fouten in 2 min).
- Mogelijke fix (nog niet uitgevoerd, ter afstemming): `logstash_system` een custom rol met `manage_logstash_pipelines` geven, of `xpack.management` op een user met dat privilege laten authenticeren.

### 12. logstash-managed 403 opgelost (optie 1)
- Verzoek: fix de 403 via optie 1 (custom rol met `manage_logstash_pipelines`).
- Bevinding: `logstash_system` is een **reserved user** — ES staat alleen wachtwoordwijziging toe, geen extra rollen (`user [logstash_system] is reserved and only the password can be changed`). Ook de reserved **rol** `logstash_system` is onwijzigbaar (`Role [logstash_system] is reserved and may not be used`). Een custom rol rechtstreeks aan `logstash_system` hangen is dus onmogelijk in Elasticsearch.
- Werkende invulling in de geest van optie 1 (privilege via custom rol i.p.v. over-geprivilegieerde built-in):
  1. Custom rol **`cpm_logstash_pipelines`** aangemaakt met cluster-privileges `monitor` (voor de license-check op `/`) + `manage_logstash_pipelines`.
  2. Dedicated native user **`cpm_logstash`** aangemaakt met die rol (wachtwoord = `LOGSTASH_PASSWORD`).
  3. `config/logstash-managed/logstash.yml`: `xpack.management.elasticsearch.username` van `logstash_system` → `cpm_logstash` (monitoring-blok blijft `elastic`).
  4. `docker compose restart logstash-managed` (bind-mounted config vereist expliciete restart).
- Resultaat: **403 → 0 fouten**. `cpm_logstash` krijgt 200 op `/_logstash/pipeline` en `/`. Logstash-managed haalt nu alle 7 CPM-pipelines op uit centralized management.

### Nieuw openstaand punt (los van de 403): ontbrekende API-key env vars
- De opgehaalde CPM-pipelines verwijzen naar 3 env vars die niet in de omgeving van `logstash-managed` staan, waardoor ze niet compileren:
  - `ES_API_KEY_16ACRGCJSNMTQKKIHXYGVQ`
  - `ES_API_KEY_CDS5L0STRSISLGV2EPWJMG`
  - `ES_API_KEY_QWNBFYSWQ02USRR8P9G`
- Dit zijn per-cluster ingest API-keys die de CPM pipeline-manager normaal genereert en in de omgeving van logstash-managed injecteert. Foutmelding: `Cannot evaluate ${ES_API_KEY_...}. Replacement variable ... is not defined`.
- 7 CPM-pipelines aanwezig: `kafka-to-central`, `central_cpm-catchall-*`, `remote-a_cpm-catchall-*`, `remote-a_cpm-dedicated-*`, `remote-b_cpm-catchall-*`, `default_cpmw-catchall-*`, `default_cpmw-dedicated-*`.
- Nog niet opgelost — ter afstemming.

### 13. Ontbrekende API-key env vars opgelost + pipelines geregenereerd
- Verzoek: los ook de ontbrekende `ES_API_KEY_*` env vars op.
- Analyse van de pipeline-manager watcher (`ansible/roles/elastic_cpm/templates/watcher_cpm-pipeline-manager.json.j2`): env-var-naam = `ES_API_KEY_` + `cluster_id` (hier cluster-UUID), uppercase, `-`→`_`. De actuele key-waarde moet in de omgeving van `logstash-managed` staan (`env_file: .env`).
- Mapping (cluster_uuid → ingest-cluster → env var):
  - `CDs5L0stRsiSlgV2EPWjmg` → es-central → `ES_API_KEY_CDS5L0STRSISLGV2EPWJMG`
  - `qwnBfYSwQ02Usrr8p9G-ZQ` → es-remote-a → `ES_API_KEY_QWNBFYSWQ02USRR8P9G_ZQ`
  - `16AcRGCJSNmTqKkIHxYGvQ` → es-remote-b → `ES_API_KEY_16ACRGCJSNMTQKKIHXYGVQ`
- Op elke cluster een ingest-API-key (`cpm-ingest`, role_descriptor met data-stream schrijfrechten) aangemaakt; als `id:api_key` toegevoegd aan `docker-local/.env` (idempotent). `logstash-managed` recreated → 3 `ES_API_KEY_*` vars in container, **0 undefined-apikey fouten**.
- Bij het ophalen bleken de pipelines **stale** (hosts = oude container-ID `0517a4891119`). Daarom de CPM-keten opnieuw gedraaid met `ansible-playbook -i inventories/local/hosts bootstrap.yml` (registry-sync → scoring → routing-advisor → state-manager → stale pipelines verwijderen → pipeline-manager → stream-coverage). `ok=22, failed=0`. 15 pipelines geregenereerd (dhl/mld-varianten per cluster).
- Twee **local-vs-branch mismatches** ontdekt en local-only opgelost:
  1. **Dubbele poort** in ES-host (`https://es-central:9200:9200`, `URI is not valid`). Oorzaak: de watcher plakt altijd `:9200`, maar de lokale inventory had al `:9200`. Conventie (bevestigd via `kaposi`): `ingest_hosts` zónder poort. Fix: `ansible/inventories/local/group_vars/all.yml` → `:9200` verwijderd uit de 3 `ingest_hosts`.
  2. **Niet-bestaande Kafka-hosts** `dhl-kafka`/`mld-kafka` (branch introduceerde dual-Kafka; lokale compose heeft één `kafka`). Fix: `dhl-kafka` + `mld-kafka` als netwerk-aliassen op de `kafka`-service in `docker-local/docker-compose.yml`.
- Na herbootstrap + restart: `URI is not valid` = 0, `undefined-apikey` = 0. Geregenereerde pipeline correct: `bootstrap_servers => "dhl-kafka:9092"`, `hosts => ["https://es-central:9200"]`, `api_key => "${ES_API_KEY_CDS5L0STRSISLGV2EPWJMG}"`.

### 14. `ssl_certificate_verification` → `ssl_verification_mode => none` + orphan-cleanup
- Keuze gebruiker: `ssl_verification_mode => none` (werkt lokaal direct; afweging: verzwakt verificatie voor omgevingen die de gedeelde template gebruiken).
- Gedeelde template-JSON aangepast (`cpm-pipeline-template-catchall.json` en `-dedicated.json`): `ssl_certificate_verification => true` → `ssl_verification_mode => none`.
- Belangrijk: `bootstrap.yml` re-seedt de templates **niet** (dat doet alleen `site.yml`/de `elastic_cpm` role). Daarom de twee templates direct in ES ge-PUT (`cpm-pipeline-templates/_doc/{catchall,dedicated}`), daarna `bootstrap.yml` opnieuw → pipelines geregenereerd met `ssl_verification_mode => none`.
- Twee stale **`default_cpmw-*`** pipelines bleven fouten geven (oude ssl-setting). Dit zijn orphans van het **workflows**-systeem (`cpmw-*`, met "w"), niet van de watcher-based `cpm-*` bootstrap. De bootstrap-delete-filter (`'cpm-' in key`) matcht `cpmw-` niet, dus ze werden nooit opgeruimd. Beide handmatig verwijderd via `DELETE /_logstash/pipeline/<id>`.
- Logstash-managed herstart → **13 actieve pipelines, 0 reload-failures, 0 fouten**. Pipelines consumeren uit Kafka (idle: nog geen load/data in de topics → 0 events, verwacht).

### Eindresultaat CPM pipeline-flow
- 13 CPM-pipelines draaien in `logstash-managed` (catchall dhl+mld per cluster + dedicated remote-a), allemaal met correcte host (`https://<cluster>:9200`), Kafka (`dhl-kafka`/`mld-kafka` → lokale broker via alias), API-key (`${ES_API_KEY_<uuid>}`) en `ssl_verification_mode => none`.
- **Cosmetische restrisico (niet opgelost):** door hergebruikte `cpm-pipeline-state` (321 entries) uit de vorige sessie bestaan dubbele pipelines met verschillende `dc`-prefix (bv. `central_*` én `dc-central_*`, `remote-a_*` én `dc-a_*`). Niet brekend (zelfde `group_id` → Kafka balanceert), maar redundant. Op te schonen door de stale pipeline-state te wissen en state-manager + pipeline-manager opnieuw te draaien.

### 15. CPM Kibana-plugin bouwen + installeren (in uitvoering)
- Verzoek: installeer de Kibana-plugin uit `kibana_plugin/`.
- De plugin moet tegen de **exacte** Kibana-versie gecompileerd worden. Manifest stond op `8.19.16`, maar Kibana draait `9.4.2`. Bijgewerkt:
  - `kibana_plugin/cpm/kibana.json` → `kibanaVersion: 9.4.2`
  - `kibana_plugin/cpm/package.json` → `kibana.version: 9.4.2`
- Geen prebuilt zip aanwezig (`build/` bestaat niet). Git-tag `v9.4.2` van elastic/kibana bestaat (geverifieerd).
- Build gestart via `KIBANA_VERSION=9.4.2 ./scripts/build_kibana_cpm_plugin_docker.sh` (Docker-builder, geen lokale Node/Yarn nodig): kloont Kibana v9.4.2, `yarn kbn bootstrap` + `build-shared`, dan `yarn build` → `kibana_plugin/build/cpm-9.4.2.zip`. Duurt ~15-20 min; risico op 8.x→9.x API-breaks in de plugin-source.
- **Build-verloop:**
  - Eerste poging via het script faalde op `mkdir kibana_plugin/build` ("Operation not permitted" — schrijfrechten op de output-dir), niet de build zelf.
  - Rebuild-poging faalde op de `yarn kbn bootstrap`-layer ("Commands cannot run with an incompatible environment" — flaky yarn/corepack/netwerk).
  - Ontdekt: er bestaat al een gebouwd image **`cpm-kibana-plugin-build:9.4.2`** (8 dagen oud, uit eerdere sessie) met een succesvol gebouwde plugin. Een gefaalde rebuild vervangt dat getagde image niet.
  - Zip geëxtraheerd uit het bestaande image naar de scratchpad: `docker run --rm -v <scratchpad>:/output cpm-kibana-plugin-build:9.4.2` → `cpm-9.4.2.zip` (41 KB). Manifest in de zip geverifieerd: `id: cpm`, `version: 1.0.6`, `kibanaVersion: 9.4.2` (matcht de draaiende Kibana).
- **Installatie:**
  - `docker cp cpm-9.4.2.zip dod-elastic-kibana-1:/tmp/cpm.zip`
  - `docker exec dod-elastic-kibana-1 bin/kibana-plugin install file:///tmp/cpm.zip` → "Plugin installation complete".
  - `bin/kibana-plugin list` → `cpm@1.0.6`.
  - `docker compose restart kibana`.
- **Verificatie:**
  - Kibana **healthy** (~15s). Plugin `cpm` staat in Kibana's "Setting up [193] plugins" én "Starting [193] plugins" lijst → geladen zonder cpm-fouten.
  - Server-side routes geregistreerd: `GET /api/cpm/access` bestaat (HTTP 400 "exists but is not available with the current configuration" bij kale basic-auth — normaal; endpoint verwacht een ingelogde browsersessie). Plugin-bundle wordt geserveerd (302 auth-redirect).
  - `kibana_system` auth → 200 (eerdere reset intact).
- **Resultaat:** CPM-plugin geïnstalleerd en geladen. UI: **Stack Management → Ingest → Cluster Pipeline Manager**. Visuele bevestiging in de browser is nog niet gedaan (vereist login).
- Manifest-wijzigingen (`kibana.json`, `package.json` → 9.4.2) staan uncommitted op `mod-imre3006`.

### 16. Catchall-pipelines werden niet gevuld — watcher-catchall-tak bijgewerkt
- Klacht: gegenereerde Logstash-pipelines hielden onvervangen `__TOKEN__`'s (`__KAFKA_BOOTSTRAP_DHL__`, `__TOPIC__`, `__DATASET__`, `__NAMESPACE__`).
- Diagnose: **alle 12 catchall-pipelines BAD, dedicated OK**. De catchall-tak van de pipeline-manager watcher (`ansible/roles/elastic_cpm/templates/watcher_cpm-pipeline-manager.json.j2`) gebruikte nog de oude tokens `__KAFKA_BOOTSTRAP__` + `__TOPICS_LIST__` (single-Kafka, topic-lijst, 2 pipelines/cluster), terwijl de gecommitte catchall-template al de dual-Kafka/single-topic vorm heeft (net als dedicated). Onafgemaakte refactor: dedicated-tak was bijgewerkt, catchall-tak niet. Geen "oude vorm" in git om naar terug te draaien.
- Keuze gebruiker: **watcher-catchall-tak bijwerken** naar dual-Kafka/per-dataset (net als dedicated).
- Uitgevoerd via Python-patch op de j2 (Painless als escaped JSON-string veilig gedecodeerd → aangepast → her-geëncodeerd; backup in scratchpad):
  - Catchall-tak herschreven: één pipeline **per dataset** met beide Kafka-inputs; vervangt `__KAFKA_BOOTSTRAP_DHL__/__MLD__`, `__TOPIC__`, `__DATASET__`, `__NAMESPACE__`, `__GROUP_ID__`, `__CONSUMER_THREADS__`, `__ES_HOSTS__`; pipeline-id `<dc>_cpm-catchall-<dataset>-<cid>`; catchall `pipeline_settings`.
  - `desiredPipelineIds`-cleanup per-dataset gemaakt; oude `-dhl-`/`-mld-` catchall-ids naar `inactivePipelineIds` (worden opgeruimd).
- Gedeployed via `site.yml` (ES compileerde de Painless zonder fouten: `ok=62, failed=0`), pipelines geregenereerd via `bootstrap.yml`.
- Resultaat: **22 pipelines (21 catchall per-dataset + 1 dedicated), 0 met onvervangen tokens**. Oude dhl/mld catchall-pipelines opgeruimd. Voorbeeld: `central_cpm-catchall-vmware-CDs...` met `topics => ["logs-vmware"]`, `hosts => ["https://es-central:9200"]`, `data_stream_dataset => "vmware"`, dual-Kafka input.

### Volgend openstaand punt: `${VAR_*}` Logstash env-vars
- De templates gebruiken nu env-var-placeholders die in de omgeving van `logstash-managed` moeten staan, anders compileren de pipelines niet:
  `${VAR_BOOTSTRAP_SERVERS_DHL}`, `${VAR_BOOTSTRAP_SERVERS_MLD}`, `${VAR_API_KEY}`, `${VAR_CLIENT_ID}`, `${VAR_SSL_KEYSTORE_LOCATION}`, `${VAR_SSL_KEYSTORE_PASSWORD}`, `${VAR_SSL_TRUSTSTORE_LOCATION}`, `${VAR_SSL_TRUSTSTORE_PASSWORD}`.
- Aandachtspunt in de templates (door gebruiker gezet, niet aangepast): `VAR_CA_TRUSTED_FINGERPRINT => "${{ VAR_CA_TRUSTED_FINGERPRINT }}"` gebruikt een VAR-naam als optie-**naam** in de elasticsearch-output én dubbele-accolade `${{ }}`-syntax — lijkt geen geldige Logstash-output-optie.
- Nog niet opgelost — ter afstemming.

### 17. Catchall gecorrigeerd: stale state opgeschoond + bundeling i.p.v. per-dataset
Klacht: catchall moet ALLE niet-dedicated topics doen, en elke topic mag maar op één cluster zitten. De per-dataset aanpak uit stap 16 bleek fout.

**Bevinding A — vervuilde state (duplicatie):** `cpm-pipeline-state` bevatte per cluster twee `dc`-waarden: actueel (`dc-central`/`dc-a`/`dc-b`, 310 entries, 11:11u) én stale (`central`/`remote-a`/`remote-b`, 11 entries, 00:15u van vorige sessie). Verschillende dataset-parsing (`vmware` vs `logs-vmware`) → verschillende `_id`'s → beide bleven bestaan → dubbele pipelines voor dezelfde topic op dezelfde cluster. **Fix:** met toestemming van gebruiker de 11 stale entries gericht verwijderd (`delete_by_query` op `dc.keyword` in `central`/`remote-a`/`remote-b`). Regenereren nadien.

**Bevinding B — per-dataset verliest dekking:** per-dataset pipeline-IDs (`<dc>_cpm-catchall-<dataset>-<cid>`) faalden op datasets met punten (`elastic_agent.metricbeat`, `auditd.log`) — Logstash pipeline-IDs met punten worden geweigerd. Van 66 datasets per cluster kwamen er maar ~7 door. De 3 clusters hebben bovendien **disjuncte** datasets (98+97+115 = 310), dus elke topic hoort al bij één cluster.

**Fix (keuze gebruiker): bundelen.** Watcher-catchall-tak herschreven naar **één gebundelde pipeline per cluster** (dual-Kafka) die ALLE niet-dedicated topics als topic-lijst consumeert; pipeline-id `<dc>_cpm-catchall-<cid>` (geen dataset → geen punten). `desiredPipelineIds` navenant. Catchall-**template** aangepast: `topics => [__TOPICS_LIST__]` (i.p.v. single `__TOPIC__`) en **dynamische** output `data_stream => true` (hardcoded `index`/`data_stream_type`/`__DATASET__`/`__NAMESPACE__` verwijderd). Template ge-PUT naar ES, watcher via `site.yml` gedeployed, pipelines via `bootstrap.yml` geregenereerd.

**Resultaat geverifieerd:**
- **4 pipelines: 3 catchall (één per cluster) + 1 dedicated.**
- **0 onvervangen `__TOKEN__`'s**, **0 topics in >1 pipeline** (elke topic op één cluster), **volledige dekking** (0 state-topics ontbreken; bv. dc-a bundelt 97 topics).
- Catchall-input = beide Kafka-bootstraps; output = dynamische `data_stream => true`.

_(Stap 16 — per-dataset — is hiermee vervangen.)_

### Openstaand (ongewijzigd): `${VAR_*}` env-vars + template-output-issues
- De pipelines refereren nog aan ongedefinieerde Logstash env-vars (`${VAR_BOOTSTRAP_SERVERS_DHL/MLD}`, `${VAR_API_KEY}`, `${VAR_CLIENT_ID}`, `${VAR_SSL_*}`) → compileren nog niet in `logstash-managed`.
- Template-output bevat nog `VAR_CA_TRUSTED_FINGERPRINT => "${{ VAR_CA_TRUSTED_FINGERPRINT }}"` (VAR-naam als optie + dubbele accolade) — geen geldige elasticsearch-output-optie. Niet aangepast.

### 18. Env-vars geprovisioneerd + template-output lokaal gemaakt
- Verzoek: provisioneer de `${VAR_*}` env-vars. Gekozen vervolgacties: per-cluster API-keys + services starten, template-output lokaal maken, orphan `logstash-managed` opruimen.
- **Env-vars** toegevoegd aan `docker-local/.env`: `VAR_BOOTSTRAP_SERVERS_DHL/MLD=kafka:9092`, `VAR_API_KEY=<es-central ingest key>`, `VAR_CLIENT_ID=cpm-logstash`, `VAR_SSL_*=` (leeg).
- **Template-output lokaal gemaakt** (beide templates, via script): verwijderd `ssl_keystore_path/password`, `ssl_truststore_path/password`, de ongeldige `VAR_CA_TRUSTED_FINGERPRINT => "${{ ... }}"`-regel, en een dubbele `action => "create"`. Behouden: `api_key => "${VAR_API_KEY}"` + `ssl_enabled => true` + `ssl_verification_mode => "none"`. Templates ge-PUT naar ES, pipelines geregenereerd → **output nu schoon** (geverifieerd: geen keystores/ca_fingerprint meer).
- **Blokkade voor de andere twee acties:** de compose (HEAD) definieert per-cluster logstash (`logstash-central/remote-a/remote-b/router`), maar hun config-bestanden **ontbreken in de working tree** (`docker-local/config/` heeft alleen `logstash-beats` + `logstash-managed`). Op `main` staan ze op een ander pad (root `config/`, andere structuur). Zonder die configs kunnen de per-cluster services niet starten → per-cluster keys wiren en `logstash-managed` verwijderen kan (nog) niet veilig. Wacht op keuze over de config-bron.

### 19. Per-cluster logstash opgezet + gestart + orphan opgeruimd
- **Configs gegenereerd** (uit de bewezen logstash-managed config): `config/logstash-central/`, `-remote-a/`, `-remote-b/logstash.yml` — identiek maar met per-cluster filter `xpack.management.pipeline.id: ["dc-central_*"]` / `["dc-a_*"]` / `["dc-b_*"]`, management via es-central met user `cpm_logstash`, en de 9.x `api.http.host`.
- **Per-cluster API-keys**: ingest-keys aangemaakt op es-remote-a en es-remote-b (es-central had er al één). In `.env` als `VAR_API_KEY_CENTRAL/REMOTE_A/REMOTE_B`. In de compose per service `VAR_API_KEY=${VAR_API_KEY_<X>}` als environment-override (gitignored keys blijven in `.env`).
- **Kafka**: de draaiende kafka was single-voter KRaft (`1@kafka:9093`); de compose wil 3-voter. Kafka gerecreëerd + kafka2/kafka3 gestart → **3-broker cluster gevormd** (3 voters, leider gekozen).
- **Orphan opgeruimd**: `logstash-managed` gestopt en verwijderd (niet meer in de compose).
- **Gestart**: `logstash-central`, `logstash-remote-a`, `logstash-remote-b`.
- **Resultaat geverifieerd** — elke instance pikt exact z'n eigen pipelines op, compileert zonder fouten:
  - logstash-central: 1 pipeline (dc-central catchall)
  - logstash-remote-a: 2 pipelines (dc-a catchall + dedicated)
  - logstash-remote-b: 1 pipeline (dc-b catchall)
  - **0 undefined-var fouten, 0 compile-fouten, 0 reload-failures, 0 ES-auth-401.** Pipelines draaien (idle: nog geen data in de Kafka-topics).

### 20. Cluster gevuld met test-data + volledige testronde + testverslag
- Verzoek: vul het cluster met test-data, test alles opnieuw grondig, en maak een testverslag (zoals de vorige keer, `TESTRESULTS-2026-06-26.md`).
- **Test-data**: `requests` in de venv geïnstalleerd, CA-cert geëxporteerd (verifieert `localhost`). Via `simulate_datastream_load.py` / `simulate_nginx_load.py` / `simulate_auditd_load.py` **76.000 docs** bulk-geïndexeerd over 6 nieuwe data streams op de 3 clusters (central: nginx-prod 35k, auditd-tst 15k; remote-a: webapp-prod 12k, nginx.access-tst 12k; remote-b: firewall-prod 12k, system-prod 10k). 0 index-errors.
- Gewacht op monitoring (nieuwe streams zichtbaar via `elasticsearch.index.name`), daarna `bootstrap.yml` → state 310→315, nieuwe streams ontdekt.
- **Testronde** (aangepast testplan; systeem onder test = `cpm-*` watchers + per-cluster logstash): INFRA, DEP, FUNC (register-sync/scoring/routing/state/pipeline-manager), E2E, SCALE, + nieuwe tests (per-cluster logstash, catchall-bundeling, topic-uniciteit, data-ingest). Resultaat: **alle uitgevoerde tests Pass** (enkele Pass\*), geen fouten.
  - Routing werkt: zwaarste stream `logs-nginx-prod` (35k) → **dedicated** op dc-a; overige streams → per-cluster catchall.
  - Dekking: alle 6 nieuwe streams gedekt; 0 topics in >1 pipeline; idempotent (315→315).
  - Per-cluster logstash: 0 compile-fouten, 0 reload-failures, 0 ES-auth-401.
- **Testverslag** geschreven: `TESTRESULTS-2026-07-17.md`.
- Kanttekeningen: trial-license verloopt **2026-07-18** (morgen); es-central 89% geheugen; **Kafka→Logstash→ES niet end-to-end getest** (geen Kafka-producer lokaal — pipelines draaien idle, test-data is direct in ES geïndexeerd).

### 21. Volledige schone herbouw: teardown → fresh → install_cpm → plugin → data
- Verzoek: verwijder de elastic-cluster, start opnieuw, installeer CPM (`install_cpm` = `site.yml`), installeer de plugin, en vul met data.
- **Teardown**: `docker compose down -v --remove-orphans` + orphan-volumes verwijderd → 0 volumes (schone lei).
- **Fresh start**: core-stack omhoog (es ×3, init, kibana, kafka ×3 + ui, logstash-beats, metricbeat ×3; nog niet de CPM-logstash/router). init exit 0; **alle auth werkt** (fresh init zette kibana_system/logstash_system correct — geen 401 dit keer). ES 9.4.2, nieuwe cluster-uuids.
- **install_cpm**: `site.yml` (`failed=0`) → 12 `cpm-*` indices, 5 ML-jobs, 7 watchers, 2 templates. Daarna `bootstrap.yml`.
- **Security**: `cpm_logstash`-rol/user opnieuw aangemaakt; verse per-cluster ingest-keys op es-central/remote-a/remote-b; `.env` `VAR_API_KEY_*` bijgewerkt.
- **Per-cluster logstash** gestart (central/remote-a/remote-b).
- **Plugin**: `cpm-9.4.2.zip` (uit bestaand build-image) geïnstalleerd in de verse kibana + restart.
- **Data**: 76.000 docs over 6 data streams op de 3 clusters (0 errors).

### Openstaand: CPM-pipelines genereren nog niet (monitoring-historie nodig)
- Na data + bootstrap: **0 gegenereerde pipelines**, `cpm-pipeline-state` ~leeg.
- Diagnose: de datastream-discovery (`cpm-state-manager`, input `dataset_rates`) is **rate-gated** — een `date_histogram` met `fixed_interval: 1h` over `now-2h` op de groei van `elasticsearch.index.total.indexing.index_total`. Op een **verse** cluster zit alle data in ~1 bucket → geen groei-over-≥2-buckets → streams gelden als "inactief" → niet toegevoegd als catchall. De CCS-remote `monitoring` is verbonden en de query **vindt de streams wél** (3 clusters × 2 datastreams); alleen de rate ontbreekt.
- Dit is de gedocumenteerde preconditie in `TESTPLAN.md` ("≥2 uur monitoring-historie + continue load"). Ook scoring/routing hangen van ML-forecasts af (historie).
- `backfill_monitoring_history.py` gedraaid (7 dagen **cluster/node**-historie voor ML), maar dat vult **geen** per-datastream `elasticsearch.index`-historie — de catchall-discovery blijft dus rate-gegated.
- Opties: (a) index-niveau monitoring-historie synthetiseren om nu te forceren, (b) continue load ~2u laten lopen, (c) laten zoals het is (fresh install klaar, pipelines vullen zich met continue ingest).

### Wijzigingen in de repo vandaag
- `docs/dagboek/2026-07-17.md` (nieuw)
- `docker-local/.env` (nieuw, gitignored) — `STACK_VERSION=9.4.2`
- `docker-local/config/logstash-beats/logstash.yml` — `http.host` → `api.http.host`
- `docker-local/config/logstash-managed/logstash.yml` — `http.host` → `api.http.host` én `xpack.management` username `logstash_system` → `cpm_logstash`
- ES security (runtime, niet in repo): custom rol `cpm_logstash_pipelines` + user `cpm_logstash` aangemaakt op es-central
- `docker-local/.env` — 3 `ES_API_KEY_*` ingest-keys toegevoegd (local, gitignored)
- `docker-local/docker-compose.yml` — `dhl-kafka`/`mld-kafka` netwerk-aliassen op `kafka`-service
- `ansible/inventories/local/group_vars/all.yml` — `:9200` verwijderd uit de 3 `ingest_hosts`
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-catchall.json` — `ssl_certificate_verification => true` → `ssl_verification_mode => none` (**gedeeld**, raakt ook kaposi/productie)
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-dedicated.json` — idem
- `kibana_plugin/cpm/kibana.json` — `kibanaVersion` `8.19.16` → `9.4.2`
- `kibana_plugin/cpm/package.json` — `kibana.version` `8.19.16` → `9.4.2`
- Kibana runtime: CPM-plugin `cpm@1.0.6` geïnstalleerd in de `kibana`-container
- `ansible/roles/elastic_cpm/templates/watcher_cpm-pipeline-manager.json.j2` — catchall-tak: **bundeling** (één dual-Kafka pipeline per cluster met topic-lijst) + `desiredPipelineIds` per cluster
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-catchall.json` — `topics => [__TOPICS_LIST__]` + dynamische output `data_stream => true` (hardcoded index/type/dataset/namespace verwijderd)
- ES data (runtime): 11 stale entries uit `cpm-pipeline-state` verwijderd (dc = central/remote-a/remote-b)
- `docker-local/.env` — `VAR_*` pipeline env-vars toegevoegd (kafka, api_key, client_id, ssl leeg)
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-catchall.json` + `-dedicated.json` — output lokaal gemaakt (ssl keystore/truststore + ca_fingerprint verwijderd, dubbele action-regel weg)
- ES security (runtime): ingest-key `cpm-logstash-var` op es-central/remote-a/remote-b
- `docker-local/config/logstash-central/`, `-remote-a/`, `-remote-b/logstash.yml` (nieuw) — per-cluster CPM-logstash configs
- `docker-local/docker-compose.yml` — per-service `VAR_API_KEY`-override op de 3 logstash-services
- `docker-local/.env` — `VAR_API_KEY_CENTRAL/REMOTE_A/REMOTE_B` toegevoegd
- Runtime: 3-broker kafka-cluster gevormd; orphan `logstash-managed` verwijderd; per-cluster logstash gestart
- `TESTRESULTS-2026-07-17.md` (nieuw) — testverslag van deze ronde
- ES data (runtime): 6 test-data streams (76k docs) op de 3 clusters
- ES security/data (runtime): ingest-API-keys `cpm-ingest` aangemaakt op es-central/remote-a/remote-b; CPM-pipelines geregenereerd via bootstrap
- Branch gewisseld naar `mod-imre3006`

