# Dagboek — 2026-08-11

## Doel van vandaag
- De lokale stack (`docker-local/`) opnieuw starten na ~3 weken stilstand.
- De CPM-keten opnieuw testen en een testverslag opleveren.

## Stappen

### 1. Stack gestart
- Alle containers stonden `Exited` (de 3 ES-containers met `137` = OOM-kill).
- `docker compose up -d` faalde op `logstash-router`: de config stond **niet** in
  `docker-local/config/` maar in de root `config/` (andere branch-layout). Docker
  had daardoor lege *directories* aangemaakt op de plek van `logstash.yml` en
  `valid_topics.yml`.
- Config gekopieerd naar `docker-local/config/logstash-router/` en de container
  opnieuw aangemaakt → **alle 18 services up** (`setup`/`init` exit 0).

### 2. Ansible opnieuw beschikbaar gemaakt
- `ansible-playbook` was nergens meer geïnstalleerd (venv verdwenen, host-python
  is inmiddels 3.14). `ansible-core` in een scratch-venv geïnstalleerd.
- `site.yml` → `failed=0`, `changed=0` (alles stond er al: 7 watchers, 5 ML-jobs,
  12 `cpm-*` indices, 2 templates, 4 dashboards).

### 3. Openstaand punt van 17-07 opgelost: 0 pipelines
- Het dagboek van 17-07 weet de lege `cpm-pipeline-state` aan een ontbrekende
  monitoring-historie ("rate-gated discovery"). Bij nalezen van
  `watcher_cpm-state-manager.json.j2` blijkt de transform **geen** groei-check te
  doen — de discovery loopt simpelweg over alle indices uit de laatste 2 uur.
- De echte oorzaak: **alle registry-entries stonden op `active: false`**, plus 3
  stale entries met cluster-uuid's van vóór de rebuild. `cpm-scoring` filtert op
  `active: true` (→ 0 clusters) en `cpm-state-manager` slaat inactieve clusters
  over (→ 0 datastreams → 0 pipelines).
- `active` wordt door **geen enkele** playbook gezet: `cpm-registry-sync` schrijft
  `false` voor nieuwe clusters en behoudt daarna de bestaande waarde. Het is dus
  een handmatige operator-actie (normaal via de CPM-plugin).
- Stale entries verwijderd, 3 live clusters op `active: true` gezet →
  `bootstrap.yml` levert direct **3 catchall-pipelines en 7 state-entries**.

### 4. Twee fouten in de gedeelde pipeline-templates gevonden en gefixt
- **Fout 1 — `manage_template`**: de ES-output installeert het `ecs-logstash`
  index-template, maar de per-cluster **ingest**-API-key heeft geen
  `manage_index_templates` → HTTP 403 → *"Failed to bootstrap. Pipeline … is going
  to shut down"*. Alle drie de Logstash-instances startten hun pipeline en
  stopten hem meteen weer; er was dus **geen enkele ingest mogelijk**.
  → `manage_template => false` toegevoegd in de `filebeat`-tak van beide templates.
- **Fout 2 — `index` + `data_stream`**: in de dedicated-template stond
  `index => "filebeat-%{[agent][version]}"` samen met `data_stream => true` in
  dezelfde output. Apart gereproduceerd met een wegwerp-Logstash:
  `LogStash::ConfigurationError: Invalid data stream configuration: ["index"]`.
  Dedicated pipelines konden dus **nooit** starten. → `index`-regel verwijderd.
- Beide bestanden zijn **gedeeld** met kaposi/productie; daar gelden dezelfde
  twee fouten.
- Na hertoepassen van de templates + `bootstrap.yml`: alle 3 de pipelines draaien,
  0 reload-failures, 0 401/403.

### 5. Keten end-to-end getest (was open sinds 17-07)
- **Catchall**: 10 events naar Kafka-topic `logs-webapp-prod` → binnen ~10 s in
  `.ds-logs-webapp-prod` op es-remote-a. Doc bevat `event.lag.31.label` =
  pipeline-id en de hostname-tag uit de filter.
- **Router**: 5 events met bekend `data_stream` naar `test-dataset` → doorgezet
  naar topic `logs-nginx-prod` → es-central. 3 events met onbekend stream →
  `dead-letter-queue` met `dlq.reason` en `dlq.intended_topic`.
- **Dedicated**: een dedicated state-entry geforceerd → pipeline correct gerenderd
  (`data_stream_dataset => "webapp"`), opgepakt door logstash-remote-a, 10 events
  geïngest. Daarna weer opgeruimd (state terug naar 7 entries, 3 pipelines).
- **Scoring**: na `cpm-forecast-trigger` geeft `cpm-scoring` echte waarden
  (20,04 / 19,22 / 19,27). Let op: scoring direct ná de forecast-trigger geeft
  nog 0.0 — de ML-forecasts zijn dan nog niet klaar.

### 6. CPM Kibana-plugin herinstalleerd
- De plugin was weg: de kibana-container is door `docker compose up -d` opnieuw
  aangemaakt en de plugin zat alleen in de oude container (niet in image/compose).
- `cpm-9.4.2.zip` uit `cpm-kibana-plugin-build:9.4.2` gehaald en geïnstalleerd →
  `cpm@1.0.6`; `/api/cpm/clusters` geeft de 3 actieve clusters terug.

### 7. Testverslag
- `TESTRESULTS-2026-08-11.md` geschreven: alle uitgevoerde tests **Pass**
  (enkele Pass\*), 1 test niet uitgevoerd (routing-suggesties onder load).

## Wijzigingen in de repo vandaag
- `docker-local/config/logstash-router/` (nieuw) — `logstash.yml`,
  `valid_topics.yml`, `pipeline/router.conf` (kopie uit root `config/`)
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-catchall.json` —
  `manage_template => false` in de filebeat-output (**gedeeld**, raakt kaposi/productie)
- `ansible/roles/elastic_cpm/files/json/cpm-pipeline-template-dedicated.json` —
  idem, plus `index`-regel verwijderd uit de data_stream-output (**gedeeld**)
- `TESTRESULTS-2026-08-11.md` (nieuw)
- `docs/dagboek/2026-08-11.md` (nieuw)
- ES data (runtime): 3 stale registry-docs verwijderd, 3 clusters op `active: true`
- Kibana runtime: CPM-plugin `cpm@1.0.6` opnieuw geïnstalleerd

## Risico's / openstaand
- `active: true` is een handmatige stap — na een verse install doet de keten niets
  zonder foutmelding. Overweeg dit in `cpm_cluster_registry` of de bootstrap-patch
  op te nemen.
- es-remote-a/-b draaien op ~99,7% van hun 1 GiB mem_limit (eerder OOM-kills).
- Trial-license verloopt **2026-08-16**.
- `valid_topics.yml` van de router is de verouderde kaposi-lijst.
- CPM-plugin zit niet in de compose/image en verdwijnt bij het opnieuw aanmaken
  van de kibana-container.
